node_modules
.next
.git
.github
out
build

# generated at build time inside the image
src/generated

# never ship the local database or local secrets
prisma/*.db
prisma/*.db-journal
.env
.env.*
!.env.example

# tooling noise
.agents
.claude
npm-debug.log*
.DS_Store
Dockerfile
docker-compose*.yml
docs

# No root-level prose in the build context. The runner stage doesn't copy any
# of it, but operational notes are exactly where a hostname or a password ends
# up by accident, and the published image is public.
*.md
SERVER-ACCESS*
